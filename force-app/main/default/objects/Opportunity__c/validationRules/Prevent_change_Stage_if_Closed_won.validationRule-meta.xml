<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_change_Stage_if_Closed_won</fullName>
    <active>true</active>
    <description>Prevent user to change the Status if it is Closed Won.</description>
    <errorConditionFormula>AND(
ISPICKVAL(PRIORVALUE( Stage__c ),&quot;Closed Won&quot;))</errorConditionFormula>
    <errorMessage>Cannot change Enquiry stage if Closed Won.</errorMessage>
</ValidationRule>
